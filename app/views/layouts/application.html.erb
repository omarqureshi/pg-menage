<!DOCTYPE html>
<html>
<head>
  <title>Content-M&eacute;nage</title>
  <%= stylesheet_link_tag "reset" %>
  <%= javascript_include_tag "jquery.min", "rails", "application" %>
  <%= csrf_meta_tag %>
</head>
<body>
  <header id="masthead">
    <h1><%= @page_title || "Content M&eacute;nage".html_safe %></h1>
    <nav>
      <ul class="no-bullets">
        <li class="<%= "active " if params[:controller] == "home" %> first">Home</li>
        <li><%= link_to "New content", new_content_path %></li>
      </ul>
    </nav>
  </header>
  <div id="wrapper" class="wrapper">
    <div id="inner-wrapper" class="inner-wrapper">
      <%= yield %>
    </div>
  </div>
</body>
</html>
